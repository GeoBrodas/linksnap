import { Stack } from '@chakra-ui/react';
import Hero from './Hero';

function Landing() {
  return (
    <Stack
      style={{
        marginTop: '3rem',
      }}
    >
      <Hero />
    </Stack>
  );
}

export default Landing;
